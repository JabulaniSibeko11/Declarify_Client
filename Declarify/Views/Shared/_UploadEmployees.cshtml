<!-- Add Employee Modal with Toggle and Dynamic Manager Dropdown -->
<div class="modal-overlay" id="addEmployeeModal">
    <div class="modal-container">
        <div class="modal-header">
            <h2>Add Employees</h2>
            <button class="modal-close" onclick="closeModal()">&times;</button>
        </div>

        <!-- Toggle Switcher -->
        <div class="modal-toggle">
            <button class="toggle-btn active" id="singleBtn" onclick="switchMode('single')">
                <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                    <path d="M16 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2" />
                    <circle cx="8.5" cy="7" r="4" />
                    <line x1="20" y1="8" x2="20" y2="14" />
                    <line x1="23" y1="11" x2="17" y2="11" />
                </svg>
                Single Employee
            </button>
            <button class="toggle-btn" id="bulkBtn" onclick="switchMode('bulk')">
                <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                    <path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z" />
                    <polyline points="14 2 14 8 20 8" />
                    <line x1="12" y1="18" x2="12" y2="12" />
                    <line x1="9" y1="15" x2="15" y2="15" />
                </svg>
                Bulk Import (Excel)
            </button>
        </div>

        <!-- Single Employee Form -->
        <form asp-action="CreateEmployee" asp-controller="Home" method="post" class="modal-form" id="singleForm" onsubmit="return validateForm()">
            @Html.AntiForgeryToken()

            <div class="form-grid">
                <div class="form-group">
                    <label>Employee Number <span class="required">*</span></label>
                    <input type="text"
                           name="EmployeeNumber"
                           id="employeeNumber"
                           required
                           placeholder="EMP001"
                           onblur="checkEmployeeNumber(this.value)" />
                    <span class="validation-message" id="employeeNumberValidation"></span>
                </div>

                <div class="form-group">
                    <label>Full Name <span class="required">*</span></label>
                    <input type="text"
                           name="Full_Name"
                           id="fullName"
                           required
                           placeholder="John" />
                </div>

                <div class="form-group">
                    <label>Surname <span class="required">*</span></label>
                    <input type="text"
                           name="Surname_Name"
                           id="surname"
                           required
                           placeholder="Doe" />
                </div>

                <div class="form-group">
                    <label>Email Address <span class="required">*</span></label>
                    <input type="email"
                           name="Email"
                           id="email"
                           required
                           placeholder="john.doe@company.org"
                           onblur="checkEmail(this.value)" />
                    <span class="validation-message" id="emailValidation"></span>
                </div>

                <div class="form-group">
                    <label>Position <span class="required">*</span></label>
                    <input type="text"
                           name="Position"
                           id="position"
                           required
                           placeholder="Senior Manager"
                           onblur="updateManagerDropdown()" />
                </div>

                <div class="form-group">
                    <label>Department <span class="required">*</span></label>
                    <input type="text"
                           name="Department"
                           id="department"
                           required
                           placeholder="Finance"
                           onblur="updateManagerDropdown()" />
                </div>

                <div class="form-group full-width">
                    <label>
                        Line Manager (Optional)
                        <span class="info-tooltip" title="Select the direct supervisor for this employee">ℹ️</span>
                    </label>
                    <select name="ManagerId" id="managerDropdown" class="manager-select">
                        <option value="">-- No Manager / CEO Level --</option>
                    </select>
                    <span class="help-text">Managers are filtered based on position and department</span>
                </div>

                <div class="form-group">
                    <label>Region (Optional)</label>
                    <input type="text"
                           name="Region"
                           id="region"
                           placeholder="e.g., North America, EMEA" />
                </div>

                <div class="form-group">
                    <label>Domain (Optional)</label>
                    <input type="number"
                           name="DomainId"
                           id="domainId"
                           placeholder="Leave empty if not applicable" />
                </div>
            </div>

            <div class="form-summary" id="formSummary" style="display: none;">
                <div class="summary-header">
                    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                        <path d="M22 11.08V12a10 10 0 11-5.93-9.14" />
                        <polyline points="22 4 12 14.01 9 11.01" />
                    </svg>
                    <span>Review Employee Details</span>
                </div>
                <div id="summaryContent"></div>
            </div>

            <div class="modal-actions">
                <button type="button" class="btn btn-ghost" onclick="closeModal()">Cancel</button>
                <button type="submit" class="btn btn-primary" id="submitBtn">
                    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                        <path d="M12 4v16m8-8H4" />
                    </svg>
                    Add Employee
                </button>
            </div>
        </form>

        <!-- Bulk Import Form -->
        <form asp-action="ImportEmployees" asp-controller="Home" method="post" enctype="multipart/form-data" class="modal-form" id="bulkForm" style="display: none;">
            @Html.AntiForgeryToken()

            <div class="upload-area" onclick="document.getElementById('excelFileInput').click()">
                <div class="upload-icon">📊</div>
                <h3>Upload Excel File</h3>
                <p>Drag and drop your Excel file here or click to browse</p>

                <input type="file"
                       name="excelFile"
                       id="excelFileInput"
                       accept=".xlsx,.xls"
                       required
                       hidden
                       onchange="handleFileSelect(event)" />

                <button type="button"
                        class="btn btn-ghost"
                        onclick="event.stopPropagation(); document.getElementById('excelFileInput').click();">
                    Choose File
                </button>

                <div class="file-preview" id="filePreview"></div>
            </div>

            <div class="csv-info">
                <div class="info-header">
                    <svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                        <circle cx="12" cy="12" r="10" />
                        <line x1="12" y1="16" x2="12" y2="12" />
                        <line x1="12" y1="8" x2="12.01" y2="8" />
                    </svg>
                    <span>Excel Format Requirements</span>
                </div>
                <ul>
                    <li><strong>File Type:</strong> .xlsx or .xls format</li>
                    <li><strong>Required Columns:</strong> EmployeeNumber, Full_Name, Surname_Name, Email, Position, Department</li>
                    <li><strong>Optional Columns:</strong> ManagerId, Region, DomainId</li>
                    <li><strong>Example Row:</strong> EMP001 | John | Doe | john.doe@company.org | Senior Manager | Finance</li>
                    <li>Manager Employee Number references the database Employee ID from existing employees</li>
                    <li>Each employee will receive login credentials via email</li>
                </ul>
                <a href="@Url.Action("DownloadEmployeeTemplate", "Home")" class="download-template">
                    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                        <path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4" />
                        <polyline points="7 10 12 15 17 10" />
                        <line x1="12" y1="15" x2="12" y2="3" />
                    </svg>
                    Download Excel Template
                </a>
            </div>

            <div class="modal-actions">
                <button type="button" class="btn btn-ghost" onclick="closeModal()">Cancel</button>
                <button type="submit" class="btn btn-primary" id="uploadBtn" disabled>
                    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                        <path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4" />
                        <polyline points="17 8 12 3 7 8" />
                        <line x1="12" y1="3" x2="12" y2="15" />
                    </svg>
                    Import Employees
                </button>
            </div>
        </form>
    </div>
</div>

<style>
    /* Additional styles for new features */
    .required {
        color: #ef4444;
    }

    .validation-message {
        display: block;
        font-size: 0.875rem;
        margin-top: 0.25rem;
        min-height: 1.25rem;
    }

        .validation-message.success {
            color: #10b981;
        }

        .validation-message.error {
            color: #ef4444;
        }

    .manager-select {
        width: 100%;
        padding: 0.75rem;
        border: 1px solid #e5e7eb;
        border-radius: 0.5rem;
        font-size: 0.9375rem;
        background-color: white;
        cursor: pointer;
        transition: all 0.2s;
    }

        .manager-select:focus {
            outline: none;
            border-color: #0891b2;
            box-shadow: 0 0 0 3px rgba(8, 145, 178, 0.1);
        }

        .manager-select option {
            padding: 0.5rem;
        }

    .info-tooltip {
        cursor: help;
        margin-left: 0.25rem;
        font-size: 0.875rem;
    }

    .help-text {
        display: block;
        font-size: 0.8125rem;
        color: #6b7280;
        margin-top: 0.25rem;
    }

    .form-summary {
        background: #f9fafb;
        border: 1px solid #e5e7eb;
        border-radius: 0.5rem;
        padding: 1rem;
        margin-top: 1.5rem;
    }

    .summary-header {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-weight: 600;
        color: #1f2937;
        margin-bottom: 0.75rem;
    }

    #summaryContent {
        font-size: 0.875rem;
        color: #4b5563;
    }

        #summaryContent p {
            margin: 0.25rem 0;
        }
</style>
